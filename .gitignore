# Build
/.build/
/SourcePackages/
/dist/
*.xcodeproj
# Xcode / SwiftPM per-user state (created when opening in Xcode)
.swiftpm/
xcuserdata/

# Editor / OS cruft
.vscode/
.idea/
.DS_Store
*.swp

# Mactions runtime state lives in ~/.mactions, never in the repo
